service: ecommerce

provider:
  name: aws
  runtime: nodejs20.x

functions:
  hello:
    handler: src/handler.hello
    events:
      - http:
          path: hello
          method: get
          cors: true
